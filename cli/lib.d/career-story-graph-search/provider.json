{
	"providerName": "CareerStoryGraph",
	"description": "RAG search over CareerStoryGraph documents (Neo4j)",
	"_note": "No startup needed. Docker --restart unless-stopped keeps containers running. First-time init: indexTextGraphForMilo -start --assetsDir=./assets",
	"startupTimeout": 30,
	"tools": [
		{
			"definition": {
				"name": "career_story_graph_search",
				"description": "Search CareerStoryGraph documents by semantic similarity. Returns the most relevant text chunks matching the query.",
				"input_schema": {
					"type": "object",
					"properties": {
						"query": {
							"type": "string",
							"description": "Natural language search query"
						},
						"limit": {
							"type": "integer",
							"description": "Max results (default 10)",
							"default": 10
						}
					},
					"required": [
						"query"
					]
				}
			},
			"cli": {
				"command": "node careerStoryGraphSearch.js",
				"positionalArgs": [
					"query"
				],
				"flagArgs": {
					"limit": "--limit"
				}
			}
		}
	]
}