; Template defaults — -start substitutes <!tokens!> on first init.
; All values are overridable after init by editing the generated .ini.

[neo4j]
containerName=rag_<!providerName!>
boltPort=<!boltPort!>
httpPort=<!httpPort!>
password=<!generatedPassword!>
dataDir=<!dataDir!>

[embedding]
voyageApiKey=<!voyageApiKey!>
embeddingModel=<!embeddingModel!>
embeddingDimensions=<!embeddingDimensions!>

[search]
topK=10
